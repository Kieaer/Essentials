package essentials;

public class EssentialAI {
    public static void main(){
        /*
        Events.on(EventType.TileChangeEvent.class, e -> {
            Thread t = new Thread(() -> {
                for(int x = 0; x < world.width() / 8; x++){
                    for(int y = 0; y < world.height() / 8; y++){
                        e.tile.drawx();
                    }
                }
                Global.log("scanned!");

            });
            t.start();
        });

         */
    }
}
